server.port=8080
server.servlet.session.timeout=15m

# MongoDB Atlas
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://admin:agenda2025@agenda-tutorias.r9kpx42.mongodb.net/agendatutorias?retryWrites=true&w=majority&appName=agenda-tutorias}

# MySQL
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/agendatutorias?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Thymeleaf
spring.thymeleaf.cache=false

# Email from address
app.mail.from=${MAIL_USERNAME:agendatutorias@gmail.com}

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false

# Gmail SMTP (SSL)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=${MAIL_USERNAME:agendatutorias@gmail.com}
spring.mail.password=${MAIL_PASSWORD:njuhvdjymljzypmd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# OAuth2 Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:858478243338-66b5km6u0qk7flu79793btvo33ojg07k.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8080/login/oauth2/code/google}